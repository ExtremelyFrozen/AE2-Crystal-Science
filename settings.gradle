pluginManagement {
    repositories {
        maven { url = 'https://repo.expandium.net/releases' }
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
